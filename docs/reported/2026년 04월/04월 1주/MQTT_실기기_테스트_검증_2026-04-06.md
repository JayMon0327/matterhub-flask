# MQTT 실기기 테스트 검증

## 이슈 요약

MQTT 연결 안정성 패치(Phase 1~4)를 실 기기에 적용하여 검증.
`konai/20260211-v1.1` 브랜치 동일 환경에서 패치 적용 후 정상 동작 확인.

## 테스트 환경

| 항목 | 값 |
|------|-----|
| 장비 IP | 192.168.1.101 |
| OS | Ubuntu 24.04.4 LTS |
| Python | 3.12.3 |
| 배포 브랜치 | konai/20260211-v1.1 (commit: 1e5dc5e) |
| .deb 패키지 | matterhub_2026.04.01-1e5dc5e_arm64.deb |
| matterhub_id | whatsmatter-nipa_SN-1775453340 |
| 코드 모드 | .pyc (source 삭제) |

## 변경 파일 목록

| 파일 | 변경 유형 |
|------|-----------|
| `/opt/matterhub/app/mqtt.pyc` | 패치 적용 |
| `/opt/matterhub/app/mqtt_pkg/runtime.pyc` | 패치 적용 |
| `/opt/matterhub/app/mqtt_pkg/publisher.pyc` | 패치 적용 |
| `/opt/matterhub/app/mqtt_pkg/state.pyc` | 패치 적용 |

## 수정 내용

`.py` 파일을 장비에 전송 → `.pyc` 컴파일 → `.py` 삭제 방식으로 패치 적용.
패치 전 `.pyc` 파일은 `/opt/matterhub/app/.patch_backup_20260406_143201/`에 백업.

## 배포 여부

- 테스트 장비(192.168.1.101)에 배포 완료

## 테스트 결과

### 1. 패치 코드 로딩 검증 (Python import 확인)

| 모듈 | 항목 | 결과 |
|------|------|------|
| mqtt.py | `CONNECTION_CHECK_INTERVAL` | 6 ✅ |
| mqtt.py | `_wait_for_network` | 존재 ✅ |
| mqtt.py | `_connect_with_service_retry` | 존재 ✅ |
| runtime.py | `RECONNECT_BACKOFF_THRESHOLD` | 5 ✅ |
| runtime.py | `RECONNECT_BASE_DELAY` | 10 ✅ |
| runtime.py | `RECONNECT_MAX_DELAY` | 300 ✅ |
| runtime.py | `_pending_resubscribe` | 존재 ✅ |
| runtime.py | `MAX_RECONNECT_ATTEMPTS` | **제거됨** ✅ |
| publisher.py | `is_connected` 체크 | 존재 ✅ |
| state.py | `_log_disconnected_once` | 존재 ✅ |

### 2. 서비스 상태

| 서비스 | 상태 |
|--------|------|
| matterhub-api | active ✅ |
| matterhub-mqtt | active ✅ |
| matterhub-rule-engine | active ✅ |
| matterhub-notifier | active ✅ |
| matterhub-update-agent | active ✅ |
| NRestarts | 0 (crash 없음) ✅ |

### 3. 로그 확인

패치 후 MQTT 로그에서 확인된 새로운 패턴:

```
[MQTT][INIT] network_ready=true                  ← Phase 1: 네트워크 대기 동작
[MQTT][CONNECT] attempting connection try=1/5     ← 1회 시도로 성공
[MQTT][CONNECT][OK] connected to broker           ← 정상 연결
[MQTT][INIT] matterhub_id=whatsmatter-nipa_SN-... ← matterhub_id 정상 로드
[MQTT][SUBSCRIBE] complete total=2 success=2      ← 구독 정상
```

### 4. 알려진 이슈 (패치 무관)

- `코나이 bootstrap: 로컬 API 실패 HTTP 502` — Home Assistant 연결 관련. MQTT 패치와 무관.
- `.env`에서 `matterhub_id` 누락 → 수동 복원 완료.

### 5. 결론

- Phase 1~4 모든 패치가 .pyc 환경에서 정상 동작
- 서비스 crash 없음 (NRestarts=0)
- MQTT 연결 1회 시도로 성공
- 네트워크 대기 로직 정상 동작 확인
